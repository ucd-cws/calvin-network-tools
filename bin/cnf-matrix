#! /usr/bin/env node
var program = require('commander');

program
  .version('0.0.1')
  .option('-f, --format <csv|tsv>', 'Output Format',/^(csv|tsv)$/,'tsv');

program.parse(process.argv);

console.log('Format:'+program.format);
console.log('Inputs:'+program.args);
